; the first attempt returns the list in reverse order, because it adds the
; successively squared items to the front of theh list, so it returns
; in reversed order.
;
; the second attempt creates two many layers of pairs, it would come out
; (1 (4 (9 (16 ...))))
