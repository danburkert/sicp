; data directed style:
; To add data types, the appropriate new functions should be registered in the
; dispatch table.  To add a new function, a new column must be added to the
; dispatch table.
;
; Message passing style:
; To add data types, the data type must be defined.  To add a new function, the
; function must be added to each existing data type.
